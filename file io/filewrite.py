with open("write.txt", 'w') as writer:
    writer.write("What are you doing bitch")
    # if we open the file now all the previous content of the file will be removed
    #when succesfull the write function returns the no of character it wrote
