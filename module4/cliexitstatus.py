# Exit statuses (or return codes) tell you whether a program ran successfully or not.
# An exit status of 0 generally means success.
# An exit status other than 0 usually indicates an error.
# You can check the exit status of the last command in the shell using $?.
# In Python, you can control the exit status of your script using sys.exit(value). If you don't specify a value, Python will exit with 0 by default.

# The list of arguments are stored in the sys module.

# in order to check the return / exit status of a program we ue echo $? 