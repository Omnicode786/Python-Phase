mynumber = input("Enter: ")
print(mynumber)
# // now if we were to add a nuber let say a mathematical operation it wil not do anything like that as its a string but if we want to do it
#  we can use the eval function

print(eval(mynumber))
# this way it is evaluated